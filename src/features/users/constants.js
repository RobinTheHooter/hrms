// Role options come from the backend (/meta/options). Only styling stays here.
export const roleBadgeVariant = {
  admin: 'destructive',
  hr: 'default',
  consultant: 'info',
  hiring_manager: 'warning',
  candidate: 'secondary',
}
