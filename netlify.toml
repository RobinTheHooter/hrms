[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: let React Router handle client-side routes so refreshing
# a deep link (e.g. /employees) doesn't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
